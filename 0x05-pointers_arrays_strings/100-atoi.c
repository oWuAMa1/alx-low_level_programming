#include "main.h"
#include <stdlib.h>
#include <ctype.h>
 #include <string.h>
/**
*_atoi - converts str to integer
*@s : string
*Return: the string converted to an integer
*/
int _atoi(char *s)
{
	int str_to_int = atoi(s);
	return (str_to_int);
}
